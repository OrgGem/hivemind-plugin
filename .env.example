# ──────────────────────────────────────────────────────────────
# HiveMind WebUI — Environment Configuration
#
# Copy to .env and adjust values. All variables are optional;
# defaults are shown below.
# ──────────────────────────────────────────────────────────────

# ─── Server ───────────────────────────────────────────────────
# HTTP port for the integrated backend+frontend server
HIVEMIND_PORT=3000

# Bind address (use 0.0.0.0 for container, 127.0.0.1 for local-only)
HIVEMIND_HOST=0.0.0.0

# Project root directory to manage (default: current working directory)
# HIVEMIND_PROJECT_ROOT=/app/project

# Directory containing built Vue SPA (leave empty for embedded fallback)
# HIVEMIND_STATIC_DIR=

# Log level: debug | info | warn | error
HIVEMIND_LOG_LEVEL=info

# ─── Default Governance Settings ──────────────────────────────
# Used when initializing new projects via the WebUI

# Governance mode: permissive | assisted | strict
HIVEMIND_DEFAULT_GOVERNANCE_MODE=assisted

# Language: en | vi
HIVEMIND_DEFAULT_LANGUAGE=en

# Automation level: manual | guided | assisted | full | coach
HIVEMIND_DEFAULT_AUTOMATION_LEVEL=assisted

# Expert level: beginner | intermediate | advanced | expert
HIVEMIND_DEFAULT_EXPERT_LEVEL=intermediate

# Output style: explanatory | outline | skeptical | architecture | minimal
HIVEMIND_DEFAULT_OUTPUT_STYLE=explanatory

# Constraints
HIVEMIND_DEFAULT_REQUIRE_CODE_REVIEW=false
HIVEMIND_DEFAULT_ENFORCE_TDD=false
